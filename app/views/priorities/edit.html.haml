= render partial: "shared/errors", locals: {obj: @priority}

%h2 Editar la prioridad

%p Ingresa los datos de la prioridad a editar
= render "form"

%p= link_to "Borrar", priority_path(@priority), method: :delete