<!DOCTYPE html>
<html>
<head>
  <title>AMT</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	<div id="topnav">
  <ul id="topnavlist">
    <li><%= link_to "Home", root_path %></li>
    <% if current_user.present? %>
      <li><%= link_to "Resultados", result_path(current_user.id) %></li>
      <li><%= link_to "Mi Perfil", creator_petitions_path(current_user.id) %></li>
      <li><%= link_to "Logout", logout_path, method: :delete, class: "logout" %></li>
    <% else %>
      <li><%= link_to "Resultados", results_path %></li>
      <li><%= link_to "Login", login_path %></li>
    <%end%>  
  </ul>
	</div>

<%= yield %>

</body>
</html>
