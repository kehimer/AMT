= render partial: "shared/errors", locals: {obj: @amt_type}

%h2 Editar tipo AMT

%p Ingresa los datos del tipo AMT a editar
= render "form"

%p= link_to "Borrar", amt_type_path(@amt_type), method: :delete