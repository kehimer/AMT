%h2 Tipos AMT
%p Listado de Tipos AMT

- if flash[:notice].present?
  .notice= flash[:notice]

- if @technologies.any?
  %table{border: "1"}
    %tr
      %th ID
      %th Titulo
      %th Summary
    = render @technologies
- else
  %em No hay ningun tipo AMT agregado