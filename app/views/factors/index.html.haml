%h2 Factores
%p Listado de Factores

- if flash[:notice].present?
  .notice= flash[:notice]

- if @factors.any?
  = render partial: "shared/filter_form", locals: {form_path: factors_path}
        
  .js_filtered_table
    = render partial: "table", locals: {factors: @factors}
- else
  %em No hay ningun factor agregado

%p= link_to "Nuevo Factor", new_factor_path